{
  "version": "2.0",
  "app_name": "make_something",
  "stages": {
    "dev": {
      "api_gateway_stage": "api",
      "environment_variables": {
        "BACKLOG_WEBHOOK_URL": "{{ DISCORD_DEV_BACKLOG_WEBHOOK_URL }}",
        "KIBELA_WEBHOOK_URL": "{{ DISCORD_DEV_DISCORD_WEBHOOK_URL }}",
        "BACKLOG_BASE_URL": "{{ BACKLOG_BASE_URL }}",
        "PROJECT_PREFIX": "{{ PROJECT_PREFIX }}"
      }
    },
    "production": {
      "api_gateway_stage": "api",
      "environment_variables": {
        "BACKLOG_WEBHOOK_URL": "{{ DISCORD_PRO_BACKLOG_WEBHOOK_URL }}",
        "KIBELA_WEBHOOK_URL": "{{ DISCORD_PRO_DISCORD_WEBHOOK_URL }}",
        "BACKLOG_BASE_URL": "{{ BACKLOG_BASE_URL }}",
        "PROJECT_PREFIX": "{{ PROJECT_PREFIX }}"
      }
    }
  }
}
